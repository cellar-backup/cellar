# Trivy vulnerability suppressions
# Format: CVE-ID [expiry] [# comment]
#
# CVE-2026-25679: net/url IPv6 parsing bug in Go stdlib
# Affects: kubectl binary (embedded Go stdlib v1.25.7)
# Fix available in Go 1.25.8 / 1.26.1, but no kubectl release
# compiled against the patched Go runtime is available yet.
# Track: https://github.com/kubernetes/kubernetes/issues (update kubectl
# ARG KUBECTL_VERSION once a release with Go 1.25.8+ is published)
CVE-2026-25679

# CVE-2026-32280: Go stdlib chain building DoS
# CVE-2026-32282: Go Root.Chmod follows symlinks
# Affects: restic binary (embedded Go stdlib v1.25.7)
# Fixed in Go 1.25.9/1.26.2 — waiting for restic to release with patched Go.
CVE-2026-32280
CVE-2026-32282

# Added: 2026-04-21
# CVE-2026-32281: Go crypto/x509 DoS via inefficient certificate chain validation
# CVE-2026-32283: Go TLS key update message flood
# CVE-2026-35469: Go stdlib vulnerability (high severity)
# Affects: restic/kubectl binaries (embedded Go stdlib v1.25.7)
# Fixed in Go 1.25.9/1.26.2 — waiting for upstream binary releases.
# Review: check if restic/kubectl have released with Go 1.26.2+ and remove these.
CVE-2026-32281
CVE-2026-32283
CVE-2026-35469
